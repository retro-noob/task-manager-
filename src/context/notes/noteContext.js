import { createContext } from "react";

// syntax to create new conext context state hold krega note se related
const noteContext=createContext(); 
export default noteContext;   // that this context will be holding all the states related to the notes.